# STATUS.md

Daily status log. Both humans append here at the end of each coding session.
Both AI agents read this at the start of each session.

**Format per entry:**

```
## YYYY-MM-DD — <Person A or B>
- Done: <what I finished>
- Blocked: <anything blocked, or "nothing">
- Next: <what I'll pick up next session>
```

Newest entries go at the top. Keep each entry tight — 3 bullets, one line each.

---

## 2026-04-23 — Person B (evening — PR #9 live re-verification + PR queue cleared)

- Done: Three threads landed in parallel. **(1) PR #9 live re-verification:** with `origin/main` at `36e60ef` and local `mcp_server` + `a2a_agent` + ngrok tunnel re-stood up, re-ran the PO smoke test against the same workspace + patient (`82e4deff-9345-4f99-9ff3-2314362a14f5`). All three post-fix invariants passed: (a) the `hook_called_fhir_found` log line now carries `prompt_note_injected=true` (PR #9's new observability field — proves `_inject_prompt_note` took the happy path, not the silent-degradation fallback); (b) the agent's artifact text quoted `patient_id=82e4deff-...` back verbatim instead of the pre-fix "FHIR context has not been received" confabulation; (c) no raw JWT and no raw `https://app.promptopinion.ai/api/workspaces/...` URL in the response. 20s round-trip, `traceparent=5486511e1dcc911475e248a32ca87b87`, source IP `20.80.113.52` Azure US-East-2 matching `docs/po_platform_notes.md` Note #3. Task terminated `TASK_STATE_COMPLETED` cleanly — the taskId-reuse bug only manifests on PO-side follow-up messages, not first turns, so this single-turn smoke is unaffected. **(2) PO support ticket:** emailed `support@promptopinion.ai` at ~19:15 CDT with a condensed TL;DR + 5-step repro (wire evidence moved to an appendix after feedback that the first draft was too dense); tracked in `docs/po_support_drafts.md` (added in this PR) as "SENT — awaiting-reply". **(3) PR queue cleared:** reviewed + approved PRs #7 (docs cleanup), #8 (criteria schema + Cigna/Aetna lumbar MRI JSON v1), and #10 (real-FHIR fan-out). One non-blocking RxNorm spot-check on PR #10 (`10689000` = claimed tramadol SCDF, unusual 8-digit shape) prompted Kevin to run a full RxNav audit of `_RXNORM_TO_KIND` — caught **5 more wrong codes beyond the one I flagged**, the worst of which was `39993` classified as NSAID/meloxicam when it's actually zolpidem (Ambien): a wrong-bucket failure that would have silently satisfied Cigna/Aetna NSAID-trial requirements for any patient on a sleep aid. Table is now 20/20 verified against RxNav. Kevin merged PR #7 (`6ebaf78`) and PR #8 (`f1ef3d0`); PR #10 rebased to `7a49698`, awaiting his self-merge.
- Blocked: Nothing critical. PO taskId-reuse support ticket is the only open external dependency — doesn't block Week-2 (first-turn flow works, demo storyboard is single-turn) but tracked for multi-turn follow-ons.
- Next: Pick up `demo/patients/` — 3 synthetic FHIR R4 bundles encoding Patient A (happy-path) / Patient B (needs-info) / Patient C (cauda-equina red-flag) as the next unit. The clinical content is already hand-authored as markdown under `demo/clinical_notes/`; this PR encodes the same cases as FHIR JSON bundles that the extractors in `mcp_server/fhir/extractors.py` can be exercised against in CI without hitting PO. Once bundles land, re-run `make mcp-fetch-patient` locally against each one + add a 4th golden-file test asserting the exact `PatientContext` shape against the 3 bundles. Lands adjacent to Kevin's PR-B (`DocumentReference` + free-text red-flag detection); the bundles are the substrate PR-B's extractor will key off.

## 2026-04-23 — Person A (real-FHIR extraction, structured-data slice)

- Done: Replaced the Week-1 skeleton in `fetch_patient_context` with a real FHIR R4 fan-out — one shared `httpx.AsyncClient` runs 7 parallel reads/searches against the SHARP-propagated FHIR server (`Patient`/`Condition`/`MedicationRequest`/`Procedure`/`ServiceRequest`/`Coverage`/`DiagnosticReport`) and feeds them through a new `mcp_server/fhir/extractors.py` to assemble a fully populated `PatientContext`. Therapy-trial `kind` strings come from a hand-curated RxNorm→kind + CPT→kind lookup pinned to PR #8's `TherapyKind` `Literal` (silent drops on unknown codes — under-counting beats wrong-bucketing for a rule engine). Red-flag candidates surface from an ICD-10 → `canonical_label` map aligned 1:1 with `cigna.redflag.*.canonical_labels` in PR #8's Cigna JSON, including a `Z85.` prefix-match so Patient C's breast-ca history (`Z85.3`) emits `history_of_cancer` even without note-text scanning. Tests use stdlib `httpx.MockTransport` (no new deps) — 33 new passing across `test_extractors.py` (per-resource units) and `test_fetch_patient_context_fhir.py` (end-to-end fanout against inline FHIR fixtures for all 3 demo patients). Demo-mode fallback kept intact; the existing 3 smoke tests still pass. Added `make mcp-fetch-patient` wrapping the curl shape for live PO smoke (FHIR_URL/FHIR_TOKEN/PATIENT_ID env vars). Free-text red-flag detection over `DocumentReference.content` deferred to PR-B (closes Patient C's cauda-equina label set). Diff is large at ~745 lines of authored code — ~120 of those are encoded-research lookup tables (RxNorm/CPT/ICD), same spirit as PR #8's JSON; flagging size in the PR body so Sanjit can ask for a split if preferred.
- Blocked: Live PO-workspace curl pending — I have Sanjit's patient id `82e4deff-9345-4f99-9ff3-2314362a14f5` from his Week-1 spike entry (now on `main` via PR #9) but not a fresh SHARP token; tests cover the fanout exhaustively and the `make` target is wired for him to run on his next session.
- Next: PR-B (DocumentReference + clinical_notes_excerpt + free-text red-flag substring detection) once this lands. Parallel: chase Sanjit on the live curl smoke so we have one real-FHIR run logged before Week 2.

## 2026-04-23 — Person B (Week-1 Platform Spike round-trip — GREEN)

- Done: Completed Day-1 PM steps 5-7 of the platform spike end-to-end. Ran `mcp_server` on :8000 + `a2a_agent` on :8001 locally; cleared a stale `make agent` PID from earlier today blocking :8001; opened ngrok static domain `creed-goofiness-amusement.ngrok-free.dev` → `localhost:8001`. Registered the agent in my PO workspace as **"Prior Auth (Lumbar MRI)"** with `X-API-Key` + `pass token` ON. Validated the stop-the-line success gate: PO's general agent routed correctly, POSTed `message/send` with `message.metadata["https://app.promptopinion.ai/schemas/a2a/v1/fhir-context"]` carrying `fhirToken` (JWT, len=1558, sha256=`6be5b48af078`), `fhirUrl`, and `patientId=82e4deff-...`. Middleware extracted + bridged all three, fhir_hook wrote them to `callback_context.state`, agent returned Gemini-generated response in 12.5s, PO displayed it. Decoded JWT `scope` claim contained **every one of the 8 FHIR scopes** we advertise in the agent card (including the 2 optional ones). Auth works (`security_authorized key_prefix=I-v-0Q`), A2A v1 card schema validates cleanly (PO never 400'd on parse), `stateTransitionHistory:false` accepted. **No email to `support@promptopinion.ai` needed — spike gate cleared on first attempt.** Three findings from the log worth documenting: (a) LLM confabulated "FHIR context not received" despite the hook succeeding, because the instruction told Gemini to "check session state" which Gemini can't actually introspect — fixed this PR by having the hook inject a redacted system-note into `llm_request.contents` so Gemini can truthfully report receipt, and rewrote the instruction to look for that pattern; (b) PO reuses the completed task's `taskId` on follow-up auto-replies, triggering A2A v1 `-32602 terminal state` rejection — spec-correct behavior on our side, PO-side bug, logged for support ticket pre-Week-2; (c) PO backend source IP is `20.80.113.52` Azure US-East-2, agent card fetched twice on registration (validate + save), W3C `traceparent` on every request — all written up in `docs/po_platform_notes.md`.
- Blocked: Nothing. Kevin's real-FHIR extraction PR on `fetch_patient_context` is **unblocked** — my PO workspace is live with a real patient (`82e4deff-9345-4f99-9ff3-2314362a14f5`) he can curl against once he finishes importing demo patients. Task-ID reuse doesn't block the Week-1 gate or the demo storyboard (Patient B flow is single-turn with a DocumentReference upload in the middle, not chained PO auto-replies).
- Next: After this PR merges — Day 3-4 pair work, import 3 demo FHIR bundles into PO workspace with hand-authored notes uploaded as DocumentReference, then Day 5-7 solo scaffold the 3 ADK sub-agents (patient_context / criteria_evaluator / pa_letter) with pass-through handoffs + verify traces render. Also file PO support ticket with the `traceparent` id from the spike log for the taskId reuse behavior.

## 2026-04-23 — Person A (PR #6 merge, post-merge cleanup)

- Done: PR #6 (`docs/payer-criteria-research`) merged (`c150892`) — Cigna (eviCore V1.0.2026) + Aetna (CPB 0236) lumbar MRI medical-necessity criteria paraphrased, cited, and cross-checked against ACR. Two follow-up commits landed during the review cycle: (a) `479c031` added a normalized `TherapyTrial.kind` taxonomy mapping payer-specific phrasing to 13 canonical strings (responding to Sanjit's note #3 — pre-empts schema drift between `fetch_patient_context` and `match_payer_criteria`); (b) `6490f60` ran the canonical Aetna URL through a non-bot-blocked browser verification pass (closing Sanjit's note #2). The verification caught one real misattribution in §5: BoneMRI experimental-subsection Z-code exclusions (`Z01.818`/`Z01.89`/`Z08`/`Z12.x`/`Z85.x`) had been wrongly attributed to the main CPB 0236 lumbar MRI policy by the original automated extraction. Corrected in-doc with an inline annotation; would have caused the Week-2 rule engine to over-reject legitimate covered cases (e.g., `Z85.3` history of breast cancer + `M54.16` lumbar radiculopathy + 6wk PT — that's *covered* under Aetna 0236, not excluded). Caught at doc-review cost vs. golden-file-test cost. Post-merge cleanup: deleted all 5 stale local feature branches (`a2a_agent/platform-spike`, `ci/strict-fail-fast`, `demo/clinical-notes`, `docs/payer-criteria-research`, `mcp_server/week2-prep`) + the 2 remote branches GitHub didn't auto-delete on merge; pruned stale tracking refs. Repo is back to single-branch state (`main` only). Generalized the WAF-extraction lesson into `docs/po_platform_notes.md` as a reusable "manual-verification-pass workflow" entry — covers the Aetna Incapsula trick (the `es.aetna.com` mirror), the eviCore PDF size-cap gotcha, and the spot-check + verification-log pattern that future payer additions (UHC, Anthem, Humana for v2) should inherit. Extended the `docs/PLAN.md` Risk Register entry "Payer criteria accuracy + IP" to reference this workflow as the standing mitigation.
- Blocked: Critical-path blocker remains Sanjit's steps 6-7 (NGROK + register agent in PO workspace with `pass token` checked). Without a live PO workspace + at least one real patient FHIR record I can curl against, `fetch_patient_context`'s real-FHIR extraction path stays skeletal (only the demographics path is exercised; Condition / MedicationRequest / Procedure / DocumentReference / ImagingStudy → red-flag candidate detection has no live target). Sanjit's working on it; ETA his next session.
- Next: Productive path while Sanjit is on 6-7 — start `feat(criteria): payer criteria JSON v1` on a new branch (`feat/criteria-json-v1`). Concrete units: (1) `shared/criteria_schema.py` Pydantic model for the criteria JSON shape (versioning + payer + procedure + the rule sub-schemas the rule engine will consume), (2) `mcp_server/data/criteria/cigna_evicore_lumbar_mri.v1_0_2026.json` encoded from the verified Cigna section of `payer_criteria_research.md`, (3) `mcp_server/data/criteria/aetna_lumbar_mri.v2026.json` encoded from the corrected Aetna section, (4) golden-file tests in `tests/criteria/` asserting parse + a small set of representative case discriminations (happy-path covered, needs-info-missing-conservative-therapy, red-flag bypass). No `match_payer_criteria` rule engine yet — that's a follow-up PR; this one is just the data + schema so Sanjit's downstream work can pull stable types from `shared/`.

---

## 2026-04-23 — Person A (CI fail-fast, post-push fix)

- Done: First CI run on PR #5 went red once the `continue-on-error` mask came off — 8 mypy errors in `mcp_server/{fhir/context.py,server.py,main.py}` all of the "Cannot find implementation or library stub" flavor (`jwt`, `fastapi`, plus cascading `McpContext not valid as a type` / `Unused "type: ignore"` from `mcp` being silently `Any`-typed). Locally the same `mypy` was green — classic workspace-venv-drift. Root cause: `uv sync --all-extras --dev` only installs the *root* project's deps; workspace members' deps declared in `mcp_server/pyproject.toml` (fastapi, PyJWT, mcp, uvicorn, …) never land on the runner. Locally I had them because `uv run --package mcp_server ...` pulls the member in on demand. Fix: added `--all-packages` to the Install-dependencies step so CI materializes every workspace member. Reproduced the failure in a throwaway `UV_PROJECT_ENVIRONMENT=/tmp/ci-repro-venv` using the old flags (mypy: 8 errors), re-ran with the new flags (mypy: 20 files clean; pytest 3/3 pass). Added an inline comment in `ci.yml` flagging why the flag is load-bearing, so no one strips it in a later diff. This is exactly the regression the fail-fast flip was meant to catch — so in a real sense, the wrapper worked.
- Blocked: Nothing. Pushing as a fixup on `ci/strict-fail-fast`; approval should carry forward if Sanjit re-green-lights post-CI.
- Next: Same as before — merge this, then real-FHIR extraction on `fetch_patient_context`.

## 2026-04-23 — Person A (CI fail-fast)

- Done: Removed `continue-on-error: true` from the mypy CI step in `.github/workflows/ci.yml` — strict is actually clean now (11 files on main, 20 files with PR #3 merged) so the "relax during early scaffold" carve-out is obsolete and was actively hiding the kind of regression PR #2's review surfaced. Flipped the pytest step the same way, but wrapped it in a small bash script that treats exit-5 ("no tests collected") as benign — pytest returns 5 rather than 0 when the `not llm and not integration` marker filter excludes every test, and on pre-PR-#3 main `tests/` did not exist yet, so a naive flip would have broken CI. Wrapper converts exit 5 → 0 with a `::warning::` annotation so GitHub surfaces the fact that zero tests ran, but still propagates real failures (exit 1+) cleanly. Verified locally against all four scenarios: (A) exit-5 → wrapper 0 ✓, (B) exit-0 → wrapper 0 ✓, (C) exit-1 → wrapper 1 ✓, (D) `yaml.safe_load` parses the workflow cleanly. Sanjit approved with a walk-through of all 6 pytest exit codes confirming the wrapper doesn't swallow failures (exits 2/3/4 all propagate). Rebased onto main post PR #3 merge to resolve a STATUS.md conflict — both PRs added a "Person A, 2026-04-23" entry at the top; kept CI fail-fast newest-first, PR #3 fix-pass directly below, prior entries unchanged. Will drop the wrapper in a follow-up once every service directory has at least one always-matching test.
- Blocked: Nothing. Approval banked on the pre-rebase SHA; GitHub carries it forward on force-push.
- Next: Merge this, then real-FHIR extraction follow-up on `fetch_patient_context` once Sanjit's PO workspace has `pass token` enabled and a real patient I can curl against.

## 2026-04-23 — Person A (PR #3 fix pass)

- Done: Addressed @Sanjit2004's review on PR #3. Blocking items: (1) amended AGENTS.md with a vendored-code carve-out to the ≤400-line rule — auditable against each package's `REFERENCE.md` so it cannot be gamed, unblocks both `a2a_agent/po_base/` and `mcp_server/fhir/` from the same cap; (2) amended `.cursor/rules/mcp-server.md` to prescribe flat `Annotated[T, Field(...)]` tool args (the current reality on `fetch_patient_context`) with the JSON-schema-ergonomics rationale spelled out, preserving a carve-out for intrinsically-structured args that still need a `shared.models` wrapper. Non-blocking items also landed in-PR: (3) documented `MCP_ALLOWED_ORIGINS` in `.env.example` under the MCP block; (4) moved `FHIR_SCOPES` to `shared/fhir_scopes.py` and had both `mcp_server/server.py` and `a2a_agent/app.py` import it — runtime check confirms `mcp_server.server.FHIR_SCOPES is shared.fhir_scopes.FHIR_SCOPES` → True, so drift between the A2A agent card and the MCP capability extension is now physically impossible; (5) added explicit JWT trust-model docstring to `get_patient_id_if_context_exists` + a new `docs/po_platform_notes.md` entry explaining that signature verification happens at the next hop (FHIR server) and our layer only uses the claim for routing; (7) replaced the `raise last_exc  # type: ignore[misc]` in `FhirClient._get` with an `assert last_exc is not None; raise last_exc` narrowing, dropping the `type: ignore`. Skipped nit #6 (real-FHIR-path test) per Sanjit's "optional for this PR" — covered by the golden-file test target in the upcoming `demo/clinical-notes` → real-extraction sequence. Also updated the tool docstring and this STATUS entry to drop the now-stale "rule amendment is a follow-up" language. `ruff check`, `ruff format --check`, `mypy --strict`, `pytest -v` all clean across 20 source files (up from 19 — `shared/fhir_scopes.py` is the new one); `a2a_app` still builds cleanly with the imported scope list.
- Blocked: Nothing. Merged as `3049c71`.
- Next: Rebase PR #5 (`ci/strict-fail-fast`) onto main to resolve STATUS.md conflict.

## 2026-04-23 — Person A (demo clinical notes)

- Done: Hand-authored 3 demo clinical progress notes under `demo/clinical_notes/` — `patient_a.md` (47F happy-path: 12wk LBP + 8 PT sessions + NSAID + muscle-relaxant + radicular symptoms, explicitly no red flags), `patient_b.md` (52M needs-info: NSAID trial documented but PT course incomplete — 1 intake visit + 3 no-shows, patient substituting YouTube stretches), `patient_c.md` (61F red-flag fast-track: ER+ breast-ca hx 7yr out on anastrozole, presents with saddle anesthesia, post-void residual 310mL with overflow incontinence, decreased rectal tone, bilateral LE weakness, night pain unrelieved by rest — textbook cauda equina). Each note is markdown with YAML front-matter carrying `DocumentReference`-ready metadata (LOINC 11506-3). Added `demo/clinical_notes/README.md` documenting format, end-to-end consumption path, and the exact red-flag vocabulary the LLM extraction will be tested against. Pure demo content — no code changes, no lock/dep changes.
- Blocked: Nothing. Parallel track while PR #3 (MCP scaffold) is in review.
- Next: Once PR #3 merges, follow-up PR to (a) delete the hardcoded `clinical_notes_excerpt` in `fetch_patient_context.py` and replace with a real `DocumentReference` search + base64-decode + truncation; (b) add a golden-file test asserting the red-flag vocabulary from `patient_c.md` is surfaced verbatim. In parallel: small CI-hardening PR flipping `continue-on-error: true` → `false` on the mypy step now that strict is actually clean.

## 2026-04-23 — Person A (MCP scaffold)

- Done: Scaffolded `mcp_server/` from `prompt-opinion/po-community-mcp`'s Python reference (commit `e19ec91`). Adapted the ~70-LOC SHARP-on-MCP transport layer inline into `mcp_server/fhir/{constants,context,client}.py` (rejected the vendor-namespace pattern used in `a2a_agent/po_base/` — surface too small, and `.cursor/rules/mcp-server.md` already prescribes the exact layout). Lifted local improvements on the FHIR client: injectable `httpx.AsyncClient` for per-request connection-pool reuse, 2x `ConnectError` retry with exponential backoff, auto-paging via `Bundle.link[next]` (upstream silently truncates at page 1). Wired FastMCP instance + capability monkeypatch in `server.py` so the `ai.promptopinion/fhir-context` extension advertises our 8 lumbar-MRI PA scopes, 1:1 aligned with `a2a_agent/app.py::fhir_scopes`. Landed `fetch_patient_context` as a Week-1 scaffold: real `Patient/{id}` read when SHARP headers are present, hardcoded Patient A fixture when absent (local curl dev), structured `FhirContextError` in the miss path. 3 pytest tests pass; `uv run mypy --strict` + `ruff check` + `ruff format --check` all clean across 19 source files. Verified live via `curl` — `initialize` returns the extension with all 8 scopes; `tools/call fetch_patient_context` returns valid `PatientContext` as both MCP `structuredContent` (2025-06 schema tier) and plaintext. Attribution + per-file adaptation log in `mcp_server/REFERENCE.md`; README rewritten from planned-structure fiction to the actual layout. Added `make mcp` + `make mcp-initialize` targets; root `pyproject.toml` mypy override for `mcp.*` (no bundled type stubs); `tests/mcp_server/` harness seeded. Flagged one rule/reality mismatch for follow-up: `.cursor/rules/mcp-server.md` says tools "accept a Pydantic input model from shared.models", but flat `Annotated[str, Field(...)]` args are strictly better because FastMCP renders the JSON schema directly from the signature — wrapper models degrade the schema. Will propose a rule amendment PR once Sanjit weighs in.
- Blocked: Nothing on the scaffold. `fetch_patient_context` FHIR path is skeletal on purpose (only demographics extracted); full extraction (Condition / MedicationRequest / Procedure / DocumentReference / ImagingStudy → red-flag candidate detection) lands in a follow-up PR once Sanjit confirms a PO workspace is stood up with `pass token` enabled + at least one real patient I can curl against. Until then, the demo fixture covers the local-dev path. Still pairing-blocked on importing the 3 demo patients + hand-authored notes into PO; Sanjit owns PO account access, I'll drive the note authoring in `demo/clinical_notes/` on a parallel branch.
- Next: Small PR to `.cursor/rules/mcp-server.md` reconciling the Pydantic-input-model rule with MCP reality (once Sanjit agrees). In parallel: (a) hand-author the 3 demo clinical notes on `demo/clinical-notes` branch (happy-path / needs-info / cauda-equina red-flag — the substrate the Week-2 Gemini reasoning pass runs over), (b) flip CI `continue-on-error: true` on mypy to fail-fast now that strict is actually clean, (c) once Sanjit has PO workspace live, real-FHIR extraction PR for `fetch_patient_context` with golden-file tests across all 3 patients.

## 2026-04-23 — Person A (PR #2 review pass)

- Done: Reviewed @Sanjit2004's `a2a_agent/platform-spike` (PR #2). First pass `REQUEST_CHANGES` on the silent `AGENT_API_KEY` bug (upstream middleware only read `API_KEYS` / `API_KEY_PRIMARY` / `API_KEY_SECONDARY`, so our documented canonical env var 401'd every authenticated request) + mypy-strict nit (43 vendored `po_base/**` errors hiding behind CI `continue-on-error: true`). Cross-checked the vendored files against `prompt-opinion/po-adk-python@6b2f742` via the GH MCP — matches upstream modulo the declared import rewrites. Sanjit's fix pass surfaced a second load_dotenv ordering bug (module-import-time env read in middleware beat `app.py`'s `load_dotenv()`) and addressed all nits; re-verified auth end-to-end locally — correct key → 200, wrong → 403, none → 401, agent-card unauth → 200 — and confirmed `override=True` defeats stale shell exports as advertised. Approved, merged as `d2afa05`.
- Blocked: Nothing. Week 1 platform spike is on `main`, `make agent` + agent card verified live, mypy strict actually runs clean now.
- Next: Start Week 2 MCP-server prep. Immediate units on a new branch (`mcp_server/week2-prep`): (1) hand-author the 3 demo-patient clinical-note markdown under `demo/clinical_notes/` (esp. Patient C cauda-equina for the red-flag/needs-info substrate), (2) scaffold `tests/` + golden-file harness so Week-2 tool PRs (`fetch_patient_context`, `match_payer_criteria`, `generate_pa_letter`) inherit it from day one. Separate tiny follow-up: flip CI `continue-on-error: true` on mypy to fail-fast now that strict passes, so regressions surface at review time.

## 2026-04-23 — Person B (PR #2 fix pass)

- Done: Addressed Kevin's review on PR #2. (1) Fixed the blocking `AGENT_API_KEY` bug in `a2a_agent/po_base/middleware.py::_load_valid_api_keys` — upstream middleware only read `API_KEYS`/`API_KEY_PRIMARY`/`API_KEY_SECONDARY`, so our documented canonical `AGENT_API_KEY` was silently ignored and every auth'd request 403'd. (2) Added `[[tool.mypy.overrides]]` for `a2a_agent.po_base.*` mirroring the ruff per-file-ignores — mypy strict now actually passes 11 source files clean instead of hiding 43 upstream errors behind CI's `continue-on-error: true`. (3) Found a **second** bug while writing the positive-auth smoke test: `load_dotenv()` in `app.py` ran after `middleware.py` had already cached `VALID_API_KEYS` from empty env; moved `load_dotenv(override=True)` to `a2a_agent/__init__.py` top-level, added `--env-file .env` to `make agent` as belt-and-suspenders. (4) Updated `REFERENCE.md` and `docs/po_platform_notes.md` with the middleware local-mod, the ruff-format normalisation note (Kevin nit #2), a Week 3 log-redaction task (nit #3), and the load_dotenv footgun writeup. (5) **Manually verified** auth end-to-end via curl: correct `X-API-Key` → HTTP 200, wrong key → 403, no key → 401. Ruff check, ruff format --check, mypy strict all clean.
- Blocked: Nothing on our side. Waiting on Kevin to re-review the follow-up commit and approve.
- Next: Once PR #2 merges → resume step 6 (register agent in PO workspace with ngrok URL), step 7 (round-trip PO chat → Gemini). Also planning a `make agent-post-smoke` target for the next PR so positive-auth is covered in CI, not just by my manual curl.

## 2026-04-23 — Person A

- Done: PR #1 merged (`3ae6909`) — Python 3.11 pinned, `mcp_server`/`a2a_agent` workspace-member stubs + `uv.lock`, 7 ruff lints resolved across `shared/models.py` (incl. `Decision` → `StrEnum`, semantically null) + scaffolding stubs. CI validated end-to-end: it blocked the broken state and went green on the fix commit.
- Blocked: Nothing for me; **critical-path blocker is `week1_spike`** — @Sanjit2004 needs to run PO signup + Gemini key + fork PO community MCP / Google-ADK reference + ngrok round-trip before any tool code can land.
- Next: While the spike is in flight — (1) hand-author the 3 demo patients' clinical-note markdown into `demo/clinical_notes/` (esp. Patient C's cauda-equina narrative, the needs-info/red-flag substrate), (2) scaffold `tests/` + golden-file harness so Week 2 tool PRs inherit it.

## 2026-04-22 — Shared

- Done: Repo bootstrapped via AI agent. Shared brain committed (AGENTS.md, CODEOWNERS, shared/, .cursor/rules/, docs/PLAN.md, docs/po_platform_notes.md, this file). CI workflow stub, docker-compose, Makefile, pyproject.toml skeletons landed.
- Blocked: Sanjit needs to provide GitHub username to replace `@sanjit-github` placeholder in CODEOWNERS. GitHub repo needs to be created under `github.com/agents-assemble` and remote added.
- Next: Both humans review this initial commit, merge it as PR #1. Then Person B leads the Week 1 Platform Spike (PO signup, Gemini key, fork reference repos, ngrok round-trip). Person A sets up CI + import demo patients once spike is done.
