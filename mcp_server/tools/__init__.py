"""MCP tools exposed to A2A sub-agents.

One file per tool. Each tool is registered in `mcp_server/server.py` via
FastMCP. Returns are `shared.models` Pydantic types (see
`.cursor/rules/mcp-server.md`).
"""
